#include <stdio.h>

/**
 * main - display lower case alphabet
 * Return: 0 and exit the program.
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
