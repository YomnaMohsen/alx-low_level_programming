#include "main.h"


/**
 *mul- multiply  2 digit numbers
 *
 * @a: first number that will be multiplicand
 * @b: second number that will be multiplier
 * Return: result of multiplication
 */
int mul(int a, int b)
{
	return (x * y);
}
