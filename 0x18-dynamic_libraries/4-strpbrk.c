#include "main.h"
#include <stdio.h>


/**
 * _strpbrk - finds the first occurrence in  s of any of bytes in str accept
 * @s: string to search in
 * @accept: substring that needs to be located
 * Return: pointer to the located  substring
 */
char *_strpbrk(char *s, char *accept)
{
	return (NULL);
}
