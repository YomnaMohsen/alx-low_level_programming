#include <stdio.h>

/**
 * main - display  with proper grammar, but the outcome is a piece of art
 * Return: main return 0 and exit the program
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}
